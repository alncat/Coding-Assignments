package strategies;

import balls.Ball;

/**
 * Updates the strategy for the ball.
 *
 */
public interface IUpdateStrategy {
	
	/**
	 * Update the state of the context ball with strategy
	 * @param context - the ball we use
	 */
	public void updateState(Ball context);
}
