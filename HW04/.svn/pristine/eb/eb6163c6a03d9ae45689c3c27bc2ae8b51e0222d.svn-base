package strategies.paint;

import java.awt.Graphics;
import java.awt.geom.AffineTransform;

import balls.Ball;

public class ADecoratorPaintStrategy extends APaintStrategy {

	/**
	 * Constructor for ADecorator strategy that calls a super
	 * on an AffineTransform object
	 * @param a
	 */
	public ADecoratorPaintStrategy(AffineTransform a) {
		super(a);

	}

	/*
	 * public ADecoratorPaintStrategy( APaintStrategy decoree) {
	 * 
	 * }
	 */

	/* (non-Javadoc)
	 * @see strategies.paint.APaintStrategy#init(balls.Ball)
	 */
	public void init(Ball host) {}

	/* (non-Javadoc)
	 * @see strategies.paint.APaintStrategy#paint(java.awt.Graphics, balls.Ball)
	 */
	public void paint(Graphics g, Ball host) {}

	/* (non-Javadoc)
	 * @see strategies.paint.APaintStrategy#paintXfrm(java.awt.Graphics, balls.Ball, java.awt.geom.AffineTransform)
	 */
	@Override
	public void paintXfrm(Graphics g, Ball host, AffineTransform af) {

	}

}
